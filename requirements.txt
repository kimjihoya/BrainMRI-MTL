# Core
numpy==2.0.2
pandas==2.3.3
scipy==1.13.1
scikit-learn==1.6.1
joblib==1.5.3
PyYAML==6.0.3
tqdm==4.67.3

# Deep learning (GPU). Install the torch build matching your CUDA:
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
pytorch-lightning==2.6.0

# Medical imaging
nibabel==5.3.3
monai==1.5.2

# Pretrained backbones (Triad / BrainIAC / BrainMVP) are NOT on PyPI —
# obtain their code + weights separately and point paths.py at them. See README.

# Explainability (codes/xai)
shap==0.49.1
matplotlib==3.9.4
